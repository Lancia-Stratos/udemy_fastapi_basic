## 計算 ##
abs() 絶対値を返す
len() 要素の個数を数える
max() 要素の最大値を数える
min() 最小値
round() 小数点以下何桁を丸めるか
sum() 要素の合計
bool() 真偽値に変換
list() リストに変換
range() 該当する整数を保持したオブジェクトを作る
int() 整数に変換
str() 文字列に変換
float() 小数に変換
tuple() タプルに変換
type() データ型を確認

## データ型関連 ##
open() ファイルを作成(もしくは読み込む)
print() 値を出力(表示)

